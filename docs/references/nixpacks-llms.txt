목적: Nixpacks를 사용할 때의 빌드 및 런타임 가정에 대한 간결한 참조다.

기록할 내용:
- 빌드 명령
- 시작 명령
- 런타임 버전
- 환경 변수
- 네이티브 의존성
- 포트 바인딩
- 캐시 디렉터리

규칙:
- 실제 배포 설정과 이 파일을 맞춘다
- 앱이 의존하는 OS 패키지나 셸 도구가 있으면 기록한다
- 에이전트가 로컬에서 런타임을 재현할 수 있도록 기본값을 문서화한다

저장소가 아직 Nixpacks를 사용하지 않는다면 이 파일은 자리표시자로 두고, 확인된 사실만 추가한다.
